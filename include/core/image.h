#ifndef IMAGE_H
#define IMAGE_H

// Pour l'instant, la structure est vide.
// Nous la remplirons dans la prochaine étape.
typedef struct {
    int width;
    int height;
    // ... plus de champs à venir
} Image;

#endif // IMAGE_H