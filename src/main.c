#include <stdio.h>

int main(int argc, char *argv[]) {
    printf("Projet imgproc démarré !\n");
    // À ce stade, nous ne faisons rien avec les arguments argc, argv
    // pour éviter les warnings "unused parameter".
    (void)argc;
    (void)argv;
    return 0;
}